# Copy these values into the server process environment; this file is only a template.
# Never commit a real master key or provider key.
AILO_SECRETS_MASTER_KEY=<32-byte-base64url-master-key>
PORT=8080
# Optional; defaults to the server working directory's data folder.
# AILO_DATA_DIRECTORY=D:\AILearningOS\server\data
