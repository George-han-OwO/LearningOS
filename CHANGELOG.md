# AILearningOS 版本记录

本文件只记录能够由现有源码、安装包、部署包或项目文档验证的内容。早期版本只保留了构建产物、没有对应的源码快照，因此不会虚构源码差异；这些版本会在 GitHub Release 中标记为“历史构建归档”。

## 1.5.5 / Server v4.1 — 2026-09-09

- 重做主页“今日待办”：默认只展示 Canvas 课程，不再直接铺开具体作业。
- 课程行显示待办数量、最近截止时间与逾期数量，点击后以轻量动画展开作业详情。
- 今日待办不再混入单词复习和 Learning Journal 入口；已完成、底栏与 Settings 保持不变。

## 1.5.4 / Server v4.1 — 2026-09-08

- 移动端底栏改为无圆角的 `Home / Words / Journal / Setting` 四项英文导航，Setting 直接位于底栏。
- 同一用户重复发起 Codex 登录时，服务器取消该用户所有未完成的近期登录，并返回结构化 HTTP 429。
- 加入五秒静默防抖；静默期内再次操作会重新计时，连续五秒无操作后才允许新建登录。
- 登录保护按 LearningOS 用户隔离；匿名流程按安全存储的随机客户端安装标识隔离，不影响其他用户或已完成登录。
- SQLite schema 升级至 16，登录静默状态可跨后端重启恢复。

## Windows / Desktop 当前源码 — 2026-09-01

- 通过官方 Codex App Server 接入 ChatGPT 订阅额度，显示两个额度窗口的用量、剩余比例与重置时间。
- 使用长期 App Server 会话，在应用前台每 2 秒增量读取 `OSS` 工作区 Codex 历史，恢复前台时立即全量校准。
- 会话镜像只保留用户消息和助手正文，排除推理、命令、工具输出与文件变更。
- 服务端 Codex 会话收件箱支持完整分页结果；AI 分析使用最近 100,000 字符，归档仍保留完整安全转录。
- 新增 `dart run tool/verify_codex_bridge.dart` 本机端到端核验命令。

## Android

### 1.3.2 — 2026-09-01

- 更新 Codex / ChatGPT 连接能力的客户端实现；Android 继续通过受信任网关使用，不在 APK 内启动本地 Codex 进程。
- 使用 Windows JDK 长路径临时目录兼容模式重新生成正式 APK。

### 1.3.1 — 2026-09-01

- 修复手机虚拟键盘弹出后遮挡输入框的问题。
- 待识别或词性为空的 Word Bank 条目进入服务器 AI 补全队列。
- 没有待处理单词或词性时，后台工作器停止轮询并断开 AI 请求链路。
- 保持与 1.3.0 相同的 Android 包名与签名，可覆盖升级。

### 1.3.0 — 2026-08-31

- 接入服务器端 API Key 加密保存与 30 秒 AI 补全队列。
- 加入银白色成功图标、深灰液态玻璃外框以及 `Success / Ready to queue` 后台入队反馈。
- 整理/导入提交后立即关闭窗口，任务转入后台处理。

### 1.2.0 — 2026-08-31

- 精简移动端主导航为“今日、Word Bank、Library”。
- 调整 Library 响应式排版，避免手机窄屏内容挤成单列竖字。
- 统一 AILearningOS 品牌与深色 Liquid Glass 视觉。

### 1.1.3 — 2026-08-28

- 历史构建归档。当前工作区没有保留这一版的独立源码快照，无法可靠还原更细的更新差异。

### 1.1.2 — 2026-08-28

- 历史构建归档。当前工作区没有保留这一版的独立源码快照，无法可靠还原更细的更新差异。

### 1.1.1 — 2026-08-28

- 历史构建归档。当前工作区没有保留这一版的独立源码快照，无法可靠还原更细的更新差异。

### 1.1.0 — 2026-08-28

- 历史构建归档。当前工作区没有保留这一版的独立源码快照，无法可靠还原更细的更新差异。

### 0.1.0 — 2026-08-26

- 旧品牌 `AIStudyOS` 的首个留存 Android Release 构建。
- 仅作历史归档，不包含当前完整功能。

## Server

### 2026-09-01-word-pos-parser-v3.1 — 2026-09-01

- 修复 DeepSeek 返回 `part_of_speech`、`part-of-speech` 或 `pos` 时词性仍停留在“待识别”的问题。
- 飞书录音摘要写入 Word Bank 时兼容相同的词性字段命名。
- 明确 AI 返回字段契约，并拒绝把“待识别”等占位文本当成有效补全结果。

### 2026-08-31-ai-key-vault-word-queue-v3-fixed — 2026-09-01

- 修复待识别/空词性的后台 AI 补全条件。
- 队列清空后停止轮询，新词或飞书导入时自动唤醒。
- API Key 以服务器加密密文保存。
- 修复包取代同日的初始 v3 包。

### 2026-08-31-ai-key-vault-word-queue-v3 — 2026-08-31

- 引入服务器端 API Key vault 与 Word Bank 后台补全队列。
- 此构建由后续 `v3-fixed` 修复包取代，保留用于历史追溯。

### 2026-08-27-ai-vault-feishu-v2 — 2026-08-27（2026-08-30 以 AILearningOS 名称重打包）

- 提供真实 AI 调用、Obsidian Vault 写入和飞书 webhook 链路。
- 加入 webhook token 校验与飞书 URL challenge 响应。
- 现有 AILearningOS 重打包文件与 `rebuilt-20260830` 文件内容相同，只发布一个附件。

### 2026-08-27-ai-vault-feishu — 2026-08-27

- 首个留存的 AI + Obsidian Vault + 飞书接入服务端构建。
- 旧品牌为 `AIStudyOS`，保留用于历史归档。
